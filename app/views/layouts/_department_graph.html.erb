<% @department.overall_query  %>
<script type="text/javascript">
  $(function () {
    var chart = new Highcharts.Chart({
      title: {text: null
          },
      chart: { 
        type: 'area',
        renderTo: 'enrollment' },
        legend: {
            enabled: true
        },
        xAxis: {
            tickPositions: [],
            type: 'category',
            categories: []
        },
        yAxis: {
          min: 0,
          title: {
            text: "enrollment"
            }
        },
      tooltip: {
        shared: true,
        formatter: function () {
          var n = this.x%10
          var y = Math.floor(this.x/10);
          var s = n == 1 ? "Spring " : n==4 ? "Summer " : n==7 ? "Fall " : "Error "
          var gd = this.points[0].point.y
          var ld = this.points[1].point.y
          var ud = this.points[2].point.y
          var total = ld + ud + gd
          return s+y+"<br/>Total: "+ total +' students<br/><span style="color:#007f46">Lower Division: </span>' + ld + ' students<br/><span style="color:#004d7c">Upper Division: </span>' + ud + ' students<br/><span style="color:#990033">Graduate Level: </span>' + gd + ' students<br/>'
      }
    },
    exporting: {
        buttons: {
            contextButtons: {
                enabled: false,
                menuItems: null
            }
        },
        enabled: true
    },

    plotOptions: {
                area: {
                    stacking: 'normal',
                    lineColor: '#666666',
                    lineWidth: 1,
                    marker: {
                        lineWidth: 1,
                        lineColor: '#666666'
                    }
                }
            },

    series: [
    {
        data: <%= @department.gd_data %>,
        color: '#990033',
        name: "Graduate Division"
    },
    {
        data: <%= @department.ld_data %>,
        color: '#007f46',
        name: "Lower Division"
    },
    {
        data: <%= @department.ud_data %>,
        color: '#004d7c',
        name: "Upper Divison"
    }

    ]
    });

    
    //overalls
    var chart = new Highcharts.Chart({
      title: {text: null
          },
      chart: { 
        //type: 'area',
        renderTo: 'overalls' },
        legend: {
            enabled: true
        },
        xAxis: {
            tickPositions: [],
            type: 'category',
            categories: []
        },
        yAxis: {
          max: 6,
          min: 0,
          title: {
            text: "enrollment"
            }
        },
      tooltip: {
        formatter: function () {
          var n = this.x%10
          var y = Math.floor(this.x/10);
          var s = n == 1 ? "Spring " : n==4 ? "Summer " : n==7 ? "Fall " : "Error "
          // var io = this.points[0].point.y
          // var to = this.points[1].point.y
          // var co = this.points[2].point.y
          return s+y+" : "+this.y
          //return s+y+'<br/><span style="color:#FFC000">Lower Division: </span>' + io + ' students<br/><span style="color:#FFC000">Upper Division: </span>' + to + ' students<br/><span style="color:#FF8000">Graduate Level: </span>' + co + ' students<br/>'
      }
    },
    exporting: {
        buttons: {
            contextButtons: {
                enabled: false,
                menuItems: null
            }
        },
        enabled: true
    },

    plotOptions: {
                area: {
                    stacking: 'normal',
                    lineColor: '#666666',
                    lineWidth: 1,
                    marker: {
                        lineWidth: 1,
                        lineColor: '#666666'
                    }
                }
            },

    series: [
    {
        data: <%= @department.to_data %>,
        color: '#8bbc20',
        name: "TA Overall"
    },
    {
        data: <%= @department.io_data %>,
        color: '#FFC000',
        name: "Instructor Overall"
    },

    {
        data: <%= @department.co_data %>,
        color: '#FF8000',
        name: "Course Overall"
    }

    ]
    });

 // // the button action
 //    //var chart = $(container).highcharts(),
 //        $overall = $(overall);
 //    $overall.click(function() {
 //        var series = chart.series[0];
 //        if (series.visible) {
 //            series.hide();
 //            // $overall.html('Show series');
 //        } else {
 //            series.show();
 //            // $overall.html('Hide series');
 //        }
 //    });

 //    // the button action
 //    //var chart = $(container).highcharts(),
 //        $availability = $(availability);
 //    $availability.click(function() {
 //        var series = chart.series[1];
 //        if (series.visible) {
 //            series.hide();
 //            // $availability.html('Show series');
 //        } else {
 //            series.show();
 //            // $availability.html('Hide series');
 //        }
 //    });

 //        // the button action
 //    //var chart = $(container).highcharts(),
 //        $effectiveness = $(effectiveness);
 //    $effectiveness.click(function() {
 //        var series = chart.series[2];
 //        if (series.visible) {
 //            series.hide();
 //            // $effectiveness.html('Show series');
 //        } else {
 //            series.show();
 //            // $effectiveness.html('Hide series');
 //        }
 //    });


 //            // the button action
 //    //var chart = $(container).highcharts(),
 //        $respect = $(respect);
 //    $respect.click(function() {
 //        var series = chart.series[3];
 //        if (series.visible) {
 //            series.hide();
 //            // $respect.html('Show series');
 //        } else {
 //            series.show();
 //            // $respect.html('Hide series');
 //        }
 //    });





  });
</script>


<input class="spoilerbutton" type="button" value="Show enrollment statistics" onclick="this.value=this.value=='Show enrollment statistics'?'Hide enrollment statistics':'Show enrollment statistics';" width=100%>
<div class="spoiler"><div>
<div width=640px align=center>
<div id="enrollment" style="height: 300px"></div>
</div>
</div></div>


<input class="spoilerbutton" type="button" value="Show department statistics" onclick="this.value=this.value=='Show department statistics'?'Hide department statistics':'Show department statistics';" width=100%>
<div class="spoiler"><div>
<div width=90% align=left>
<set1>
This graph presents the overall scores for instructors, teaching assistants and courses.<br>
<ul>
<li>Instructor Overall is the average instructor overall score of every FCQ turned in for this department <i> not taught</i> by a teaching assistant. </li> 
<li>Teaching Assistant Overall is the average instructor overall score of every FCQ turned in for this department <i> specifically taught</i> by a teaching assistant. </li>
<li>Course Overall is the average course overall score of every FCQ turned in for this department. </li>
</ul>
</set1>

<div id="overalls" style="height: 300px"></div>
</div>
</div></div>


<!-- <button id="button">Toggle visibility</button> -->